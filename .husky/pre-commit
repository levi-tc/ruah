#!/bin/sh
# Generated from governance.md by crag
# Regenerate: crag compile --target husky
set -e

# lint & format
npx @biomejs/biome check --write . || exit 1
npx @biomejs/biome check . || exit 1

# test
node --test test/*.test.js || exit 1
